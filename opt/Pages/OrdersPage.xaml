<Page x:Class="SpectrAgency.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Заказы">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📋 ЗАКАЗЫ" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="🔄 Обновить" Click="RefreshOrders_Click" Background="#FF5722" Foreground="White" Padding="10,5" Margin="2"/>
            <Button Content="✅ Завершить" Click="CompleteOrder_Click" Background="#4CAF50" Foreground="White" Padding="10,5" Margin="2"/>
            <Button Content="❌ Отменить" Click="CancelOrder_Click" Background="#F44336" Foreground="White" Padding="10,5" Margin="2"/>
        </StackPanel>

        <DataGrid x:Name="OrdersGrid" 
                  Grid.Row="2" 
                  AutoGenerateColumns="False"
                  SelectionChanged="OrdersGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding client_name}" Width="150"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding order_date}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding status}" Width="100"/>
                <DataGridTextColumn Header="Сумма, ₽" Binding="{Binding total_amount}" Width="100"/>
                <DataGridTextColumn Header="Примечание" Binding="{Binding notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>