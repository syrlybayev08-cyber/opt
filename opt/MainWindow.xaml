<Window x:Class="SpectrAgency.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Агентство Спектр" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Стиль кнопок навигации -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E64A19"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль кнопки закрытия -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F44336"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#FF5722" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <Border Grid.Row="0" CornerRadius="10,10,0,0" Background="#2E7D32">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Логотип и название -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🏗️" FontSize="32" Foreground="White" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="АГЕНТСТВО СПЕКТР" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Стройматериалы | Доставка | Ремонт | Установка" FontSize="10" Foreground="#FFE0E0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Навигация справа -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="NavOrders" Click="NavOrders_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="Заказы" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="NavProducts" Click="NavProducts_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔨" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="Товары" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="NavClients" Click="NavClients_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👥" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="Клиенты" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="NavEmployees" Click="NavEmployees_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👷" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="Сотрудники" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="CloseButton" Click="CloseButton_Click" Style="{StaticResource CloseButtonStyle}" Margin="15,0,0,0">
                            <TextBlock Text="✖" FontSize="16" FontWeight="Bold"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Контент -->
            <Border Grid.Row="1" Background="#FFF5F0F0" Padding="10">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>

            <!-- Нижняя строка -->
            <Border Grid.Row="2" CornerRadius="0,0,10,10" Background="#FFE0E0" Padding="10,5">
                <TextBlock x:Name="StatusText" Text="Агентство Спектр" FontSize="11" Foreground="#555"/>
            </Border>
        </Grid>
    </Border>
</Window>